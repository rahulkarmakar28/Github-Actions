name: 04 - Using Actions
on:
    push:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              run: actions/checkout@v7
            - name: Printing Folders
              run: ls 04-using-actions